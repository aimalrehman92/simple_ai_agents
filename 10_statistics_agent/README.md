# Statistics Agent

A simple agent for calculating descriptive statistics, z-scores, binary classification metrics, and Pearson correlation.

## Available tools

- `calculate_descriptive_statistics`
- `calculate_z_score`
- `calculate_confusion_matrix_metrics`
- `calculate_correlation`

## Run

From the project root:

```bash
python 10_statistics_agent/main.py